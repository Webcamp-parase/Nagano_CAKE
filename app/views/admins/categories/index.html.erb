<h2>ジャンル追加・一覧画面</h2>

<%= form_with [:admins, model: @category] do |f| %>
	<%= f.text_field :name %>
	<%= f.radio_button :status, :"有効" %>
	<%= f.radio_button :status, :"無効" %>
	<%= f.submit %>
<% end %>